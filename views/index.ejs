<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1 style="text-align: center;">Quora Posts</h1>
    <div class="container">
        <% for(post of posts) { %>
            <div class="card">
                <h3 class="userName">@<%= post.username  %></h3> 
                <p class="content"><%= post.content %></p>
                <a class="btn" href="/posts/<%=post.id%>">View</a>
                <a class="btn" href="/posts/<%=post.id%>/edit">Edit</a>
                <form action="/posts/<%= post.id %>?_method=delete" method="post" style="display: inline;">
                    <button class="btn">Delete</button>
                </form>
            </div>
         <%}%>
    </div>

    <div style="text-align: center;">
        <a class="createPost" href="/posts/new">Create New Post</a>
    </div>
</body>
</html>

